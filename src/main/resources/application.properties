# The redaction policy schema version supported by the bundled Phileas runtime.
# The "Test Policy" feature is only enabled when the selected schema version matches this value,
# because a given Phileas build understands exactly one schema version.
#
# TODO: once the phileas dependency is bumped to a release that includes
# ai.philterd.phileas.policy.PolicySchema, derive this at runtime from
# PolicySchema.getSupportedSchemaVersion() instead of hard-coding it here.
phileas.supported-schema-version=1.1.0

# The PhiSQL version the editor compiles with. Filled in at build time from the phisql.version
# property in the POM so the UI always reports the version actually on the classpath.
phisql.version=@phisql.version@
